# -*- mode: snippet -*-
# name: reactWithWebpackDefaults
# key: rwwd
# --
class ${1:`(file-name-sans-extension (buffer-name))`} extends React.Component {
    constructor(props) {
        super(props);

        this.state = {};

    }

    render() {
        return (
            <div>
                $0
            </div>
        );
    }
}

${1:`(file-name-sans-extension (buffer-name))`}.propTypes = {

};

export default ${1:`(file-name-sans-extension (buffer-name))`};
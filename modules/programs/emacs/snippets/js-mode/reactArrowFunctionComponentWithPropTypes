# -*- mode: snippet -*-
# name: reactArrowFunctionComponentWithPropTypes
# key: rafcp
# --
import React from 'react'
import PropTypes from 'prop-types'

const ${1:`(file-name-sans-extension (buffer-name))`} = props => {
  return (
    <div>${1:first}</div>
  )
}

${1:`(file-name-sans-extension (buffer-name))`}.propTypes = {}

export default ${1:`(file-name-sans-extension (buffer-name))`}
# -*- mode: snippet -*-
# name: flowStatelessComponent
# key: fsc
# --
// @flow
import * as React from 'react';
type Props = {
    $0
};
export const ${1:`(file-name-sans-extension (buffer-name))`} = (props: Props) => {
    return (
        <div>

        </div>
    );
};
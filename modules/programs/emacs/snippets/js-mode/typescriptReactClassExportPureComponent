# -*- mode: snippet -*-
# name: typescriptReactClassExportPureComponent
# key: tsrpce
# --
import React, { PureComponent } from 'react'

type Props = {}

class ${1:`(file-name-sans-extension (buffer-name))`} extends PureComponent<Props> {
  render() {
    return (
      <div>${1:first}</div>
    )
  }
}

export default ${1:`(file-name-sans-extension (buffer-name))`}
# -*- mode: snippet -*-
# name: reactPureComponent
# key: rpc
# --
import React, { PureComponent } from 'react';
import PropTypes from 'prop-types';

class ${1:`(file-name-sans-extension (buffer-name))`} extends PureComponent {
    render() {
        return (
            <div>
                $0
            </div>
        );
    }
}

${1:`(file-name-sans-extension (buffer-name))`}.propTypes = {

};

export default ${1:`(file-name-sans-extension (buffer-name))`};
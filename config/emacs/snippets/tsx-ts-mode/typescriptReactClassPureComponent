# -*- mode: snippet -*-
# name: typescriptReactClassPureComponent
# key: tsrpc
# --
import React, { PureComponent } from 'react'

type Props = {}

export default class ${1:`(file-name-sans-extension (buffer-name))`} extends PureComponent<Props> {
  render() {
    return (
      <>
        ${0}
      </>
    )
  }
}
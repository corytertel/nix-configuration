# -*- mode: snippet -*-
# name: reactClassExportPureComponent
# key: rpce
# --
import React, { PureComponent } from 'react'

export class ${1:`(file-name-sans-extension (buffer-name))`} extends PureComponent {
  render() {
    return (
      <div>${1:first}</div>
    )
  }
}

export default ${1:`(file-name-sans-extension (buffer-name))`}
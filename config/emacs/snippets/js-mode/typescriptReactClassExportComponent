# -*- mode: snippet -*-
# name: typescriptReactClassExportComponent
# key: tsrce
# --
import React, { Component } from 'react'

type Props = {}

type State = {}

class ${1:`(file-name-sans-extension (buffer-name))`} extends Component<Props, State> {
  state = {}

  render() {
    return (
      <div>${1:first}</div>
    )
  }
}

export default ${1:`(file-name-sans-extension (buffer-name))`}
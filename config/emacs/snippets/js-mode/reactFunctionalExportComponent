# -*- mode: snippet -*-
# name: reactFunctionalExportComponent
# key: rfce
# --
import React from 'react'

function ${1:`(file-name-sans-extension (buffer-name))`}() {
  return (
    <div>${1:first}</div>
  )
}

export default ${1:`(file-name-sans-extension (buffer-name))`}